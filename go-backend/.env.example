# Server
PORT=8080
ENVIRONMENT=production

# SQLite database file path
DATABASE_URL=./data/timespace.db

# Uploaded media directory served at /uploads/
UPLOAD_DIR=./data/uploads

# Comma-separated origins for browser access
CORS_ORIGINS=http://localhost:5173,https://your-domain.example
